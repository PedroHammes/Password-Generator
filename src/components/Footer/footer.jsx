import styles from './style.module.css'

export default function Footer() {
    return (
        <footer>
            <a href="#">Home</a>
            <a href="#">New Password</a>
            <a href="#">Profile</a>
        </footer>
    )
}